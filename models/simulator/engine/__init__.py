"""Simulator core engine (Phase 1).

MatchSimulation is the core data structure. `simulate_match()` is the public
entry point. Dixon-Coles τ correction lives in dixon_coles.py, applied in
sampling when τ != 0.
"""
